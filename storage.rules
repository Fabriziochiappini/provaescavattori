rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Consenti lettura pubblica delle immagini dei macchinari
      allow read: if true;
      // Scrittura solo per admin loggati
      allow write: if request.auth != null;
    }
  }
}
